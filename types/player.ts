export interface playerType {
    prefix: string;
    firstName: string;
    lastName: string;
    fullName?: string;
    member_id: string;
    position: string;
    is_active: boolean;
    image: string | null;
}
